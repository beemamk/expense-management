[project]
name = "my-fastapi-app"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "bcrypt>=5.0.0",
    "fastapi[standard]>=0.115.0",
    "passlib[bcrypt]>=1.7.4",
    "pwdlib[argon2]>=0.3.0",
    "python-jose[cryptography]>=3.3.0",
    "sqlalchemy>=2.0.0",
]
